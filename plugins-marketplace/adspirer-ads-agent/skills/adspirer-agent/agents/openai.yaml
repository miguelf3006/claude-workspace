interface:
  display_name: "Adspirer"
  short_description: "Paid media agent for Google, Meta, TikTok, LinkedIn, Amazon & ChatGPT Ads"

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "adspirer"
      description: "Adspirer MCP server for cross-platform ad management"
      transport: "streamable_http"
      url: "https://mcp.adspirer.com/mcp"
